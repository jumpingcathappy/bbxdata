(async () => { const API = 'https://api.lvup.gg/v2'; const token = JSON.parse(localStorage.getItem('auth#accessToken') || 'null'); if (!token) { alert('Not logged in. Please log in to lvup.gg first.'); return; } const headers = { Authorization: `Bearer ${token}` }; const get = async (path) => { const res = await fetch(API + path, { headers }); if (!res.ok) throw new Error(`${path} -> ${res.status}`); return res.json(); }; // Phase 1: list the user's brackets (paginated via body.hasNext) const brackets = []; let cursor = 0; const size = 100; for (;;) { const list = await get(`/easy-brackets/users/me2?cursor=${cursor}&size=${size}`); const body = list.body || {}; const items = Array.isArray(body.items) ? body.items : []; brackets.push(...items); if (!body.hasNext) break; cursor += size; } // Phase 2: for each bracket, fetch the detail endpoint which contains // rosters (overlaySetting.rosters) AND matchResults together. const normalized = []; for (const b of brackets) { const id = b.easyBracketId; const type = (b.bracketType || '').toLowerCase(); let matches = []; try { const detail = await get(`/easy-brackets/${id}`); const body = detail.body || {}; const rosters = (body.overlaySetting && body.overlaySetting.rosters) || []; const nameById = new Map(rosters.map((r) => [r.id, r.name || 'Unknown'])); // matchResults: each match is TWO entries (winner status:"win", loser // status:"lose") sharing the same round, referencing each other via // opponents[]. Build a map teamId -> entry, then emit one Match per // winning entry. const results = Array.isArray(body.matchResults) ? body.matchResults : []; const byTeam = new Map(results.map((r) => [r.teamId, r])); for (const r of results) { if (r.status !== 'win') continue; const oppId = (r.opponents || [])[0]; const opp = oppId ? byTeam.get(oppId) : null; matches.push({ id: `${id}-r${r.round}-${r.teamId}`, round: r.round, playerA: nameById.get(r.teamId) || 'Unknown', playerB: opp ? nameById.get(opp.teamId) || 'Unknown' : 'Unknown', winner: nameById.get(r.teamId) || 'Unknown', scoreA: r.gameWin ?? null, scoreB: opp ? opp.gameWin ?? null : null, }); } } catch (e) { console.warn('Skipping bracket', id, e); } normalized.push({ id, name: b.title || `Bracket ${id}`, type, createdAt: b.createdDatetime ? new Date(b.createdDatetime).toISOString() : '', matches, }); } const data = { exportedAt: new Date().toISOString(), brackets: normalized }; // Send to the dashboard popup opener if (window.opener) { window.opener.postMessage({ type: 'matchup-dashboard:data', data }, '*'); } // Also offer a download const blob = new Blob([JSON.stringify(data, null, 2)], { type: 'application/json' }); const a = document.createElement('a'); a.href = URL.createObjectURL(blob); a.download = 'data.json'; a.click(); console.log('Exported', normalized.length, 'brackets'); })();